<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="example.db.dao.StudentDAO">

	<insert id = "inserStudent">
  		insert into student(st_num, st_name, st_semester, st_state, st_pr_num)
  		value(#{st_num}, #{st_name}, #{st_semester}, #{st_state}, #{st_pr_num})
  	</insert>
 	 <select id="selectAllStudent" resultType = "example.db.dao.StudentVO">
 	 	select * from student where st_num #{st_num}
   </select>
  	
  	<select id = "updateStudent"
  		
  	</insert>	
  </mapper>